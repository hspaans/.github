---
name: CI

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  lint:
    name: Lint Code Base
    uses: hspaans/.github/.github/workflows/lint.yml@master
